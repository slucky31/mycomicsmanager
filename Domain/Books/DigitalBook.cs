namespace Domain.Books;

public sealed class DigitalBook : Book
{
    public DigitalBook() { }

    // No factory method exposed — not creatable via UI for now
    // No additional properties for now
}
